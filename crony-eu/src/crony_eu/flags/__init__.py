# SPDX-License-Identifier: AGPL-3.0-only
"""Flags: written rules over staged data, each with a spec before its output is used.

Constraint 8. A flag's spec lives in `crony-eu/docs/flags/<ID>-<name>.md` and
states its hypothesis, definition, innocent explanations, measured base rate and
what a journalist has to verify. Until the base rate is measured and the
maintainer has recorded it, every row a flag produces says `uncalibrated` and
none of them can enter a case packet.
"""
